name: "Fast Forward Wiki Update"

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  wiki:
    uses: php-fast-forward/dev-tools/.github/workflows/wiki.yml@main
    secrets: inherit
